<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only the splash is overridden here. Theme.FastMask no longer needs an
      API-31 variant: the window attributes that used to be duplicated in both
      files (statusBarColor, navigationBarColor, windowLightStatusBar) are gone,
      so the base definition applies on every API level.
    -->
    <style name="Theme.FastMask.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.FastMask</item>
    </style>
</resources>
